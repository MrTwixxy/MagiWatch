name: magiwatch
description: "MagiWatch Connect"
publish_to: "none"
version: 1.2.0

environment:
  sdk: ">=3.3.2 <4.0.0"

dependencies:
  dynamic_color: ^1.7.0
  flutter:
    sdk: flutter
  flutter_wear_os_connectivity: ^1.0.0
  pointycastle: ^3.7.4
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
